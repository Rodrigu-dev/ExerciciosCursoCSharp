namespace ExSemInterface.Entities
{
    //CLASSE DE DOMÍNIO
    class Vehicle
    {
        public string Model { get; set; }

        public Vehicle(string model)
        {
            Model = model;
        }

    }
}
